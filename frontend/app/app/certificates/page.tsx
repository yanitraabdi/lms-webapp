"use client";

import { RouteStub } from "@/components/RouteStub";

// /app/certificates — CSR (client component).
export default function CertificatesPage() {
  return <RouteStub name="Certificates" mode="CSR (client component)" />;
}
