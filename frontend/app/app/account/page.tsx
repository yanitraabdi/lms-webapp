"use client";

import { RouteStub } from "@/components/RouteStub";

// /app/account — CSR (client component).
export default function AccountPage() {
  return <RouteStub name="Account" mode="CSR (client component)" />;
}
