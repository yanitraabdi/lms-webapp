"use client";

import { RouteStub } from "@/components/RouteStub";

// /app/dashboard — CSR (client component).
export default function DashboardPage() {
  return <RouteStub name="Dashboard" mode="CSR (client component)" />;
}
